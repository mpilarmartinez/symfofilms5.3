{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block main %}
    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}
	<div class="bg-light border row align-items-center justify-content-center my-3 p-2">
		<div class="row">
			<div class="col-8">
				<h3 class="my-2">Registro de usuario</h3>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.displayname, {label: 'Nombre'}) }}       
        {{ form_row(registrationForm.plainPassword, {label: 'Password'}) }}
        {{ form_row(registrationForm.phone, {label: 'Telefono'}) }}
        <div class="row">
        <div class="col">
        	{{ form_row(registrationForm.city, {label: 'Poblacion'}) }}
        
        </div>
        <div class="col">
        	{{ form_row(registrationForm.country, {label: 'Pais'}) }}
        </div>
        </div>
     </div>
  </div> 
        <h3 class="mt-3">Terminos y condiciones</h3>
        <p>Este es un ejemplo de clase y los datos qye introduzcas aqui no seran tratados
        con ningun tipo de fin comercial. La sunicas notificaciones que recibiras al email son las de 
        verificar tu email y en el caso que olvideis el passwors.</p>
        
   <div class="text-center">
        {{ form_row(registrationForm.agreeTerms, {
               label: 'Acepto los terminos' 
        }) }}
        
        {{  form_rest(registrationForm) }}
        <button type="submit" class="btn btn-primary">Registro</button>
     </div>
    {{ form_end(registrationForm) }}
   </div>
{% endblock %}
