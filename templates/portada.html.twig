	
	{% extends 'base.html.twig' %}
	{% block title %}Portada{% endblock %}
	{% block main %}
		{{ parent() }}
		<p>Esta es la portada del sitio web <b>SymfoFilms</b>.</p>
		
		<p>A lo largo de las proximas semanas iremos editando este ejemplo para dotarlo de nuevas carateristicas y funcionalidad.</p>
		
	
	<div class="container d-flex my-5">
		{% for pelicula in peliculas %}
		<figure class="col m-2 text-center">
			<a href="{{  path('pelicula_show', {'id': pelicula.id}) }}">
				<img src="{{ asset(covers_directory~'/'~pelicula.caratula) }}" 
					 class="rounded img-fluid w-90 h-100 d-inline-block"
					 alt="Caratula de {{  pelicula.titulo }}"
					 title="Caratula de {{ pelicula.titulo }}">
					 
		    </a>
		    <figcaption>{{ pelicula.titulo }}</figcaption>
		 </figure>
		 {% endfor %}
	</div>
		    		
{% endblock %}


